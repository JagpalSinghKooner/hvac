---
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { Phone } from 'lucide-react';

interface Props {
  title: string;
  description: string;
  category: string;
  serviceType?: string;
  phoneNumber: string;
  phoneDisplay: string;
}

const { title, description, category, serviceType, phoneNumber, phoneDisplay } = Astro.props;
---

<section class="relative w-full bg-background py-12 md:py-16 lg:py-20">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">
      <!-- Content -->
      <div class="text-center lg:text-left space-y-6">
        <!-- Category Badge -->
        <div>
          <Badge variant="secondary" className="text-sm font-medium">
            {category}
          </Badge>
        </div>

        <!-- Title -->
        <h1 class="text-4xl md:text-5xl font-bold tracking-tight text-foreground leading-tight">
          {title}
        </h1>

        <!-- Description -->
        <p class="text-lg md:text-xl text-muted-foreground leading-relaxed">
          {description}
        </p>

        <!-- CTA Button -->
        <div class="pt-4">
          <a
            href={`tel:${phoneNumber}`}
            class="inline-flex"
          >
            <Button size="lg" className="h-14 px-8 text-lg font-semibold gap-2">
              <Phone className="h-5 w-5" />
              Call for Free Quote
            </Button>
          </a>
          <p class="mt-3 text-sm text-muted-foreground">
            {phoneDisplay}
          </p>
        </div>
      </div>

      <!-- Image Placeholder -->
      <div class="order-first lg:order-last">
        <div class="bg-muted aspect-video rounded-lg flex items-center justify-center text-muted-foreground text-sm font-medium">
          Service Hero Image — {title}
        </div>
      </div>
    </div>
  </div>
</section>
